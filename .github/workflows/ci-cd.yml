name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-cd-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint and Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm test

  deploy:
    name: Build and Deploy
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 45
    if: >
      needs.quality.result == 'success' &&
      github.ref == 'refs/heads/main' &&
      github.event_name != 'pull_request'
    env:
      APP_DIR: /home/info_platform
      HOST_PORT: ${{ vars.APP_PORT || '3000' }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: pnpm build (prebuild + nuxt build)
        run: pnpm build

      - name: Pack deploy bundle
        run: |
          tar czf deploy-bundle.tgz .output Dockerfile .dockerignore scripts/docker-deploy-remote.sh

      - name: Prepare server directory
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ vars.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ vars.DEPLOY_PORT || '22' }}
          script: |
            set -euo pipefail
            mkdir -p "${{ env.APP_DIR }}/scripts"

      - name: Upload deploy bundle to server
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ vars.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ vars.DEPLOY_PORT || '22' }}
          source: deploy-bundle.tgz
          target: ${{ env.APP_DIR }}
          overwrite: true

      - name: Extract deploy bundle on server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ vars.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ vars.DEPLOY_PORT || '22' }}
          script: |
            set -euo pipefail
            cd "${{ env.APP_DIR }}"
            tar xzf deploy-bundle.tgz
            rm -f deploy-bundle.tgz
            chmod +x scripts/docker-deploy-remote.sh

      - name: Write runtime .env on server
        uses: appleboy/ssh-action@v1.2.0
        env:
          APP_DIR: ${{ env.APP_DIR }}
          HOST_PORT: ${{ env.HOST_PORT }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_JWT_SECRET: ${{ secrets.SUPABASE_JWT_SECRET }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          POSTGRES_DATABASE: ${{ secrets.POSTGRES_DATABASE }}
          POSTGRES_HOST: ${{ secrets.POSTGRES_HOST }}
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_PRISMA_URL: ${{ secrets.POSTGRES_PRISMA_URL }}
          POSTGRES_URL: ${{ secrets.POSTGRES_URL }}
          POSTGRES_URL_NON_POOLING: ${{ secrets.POSTGRES_URL_NON_POOLING }}
          POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
        with:
          host: ${{ vars.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ vars.DEPLOY_PORT || '22' }}
          envs: APP_DIR,HOST_PORT,SUPABASE_URL,SUPABASE_KEY,SUPABASE_SERVICE_KEY,SUPABASE_SERVICE_ROLE_KEY,SUPABASE_JWT_SECRET,NEXT_PUBLIC_SUPABASE_URL,NEXT_PUBLIC_SUPABASE_ANON_KEY,POSTGRES_DATABASE,POSTGRES_HOST,POSTGRES_PASSWORD,POSTGRES_PRISMA_URL,POSTGRES_URL,POSTGRES_URL_NON_POOLING,POSTGRES_USER
          script: |
            set -euo pipefail
            umask 177
            printf '%s\n' \
              "NODE_ENV=production" \
              "HOST=0.0.0.0" \
              "PORT=3000" \
              "SUPABASE_URL=${SUPABASE_URL}" \
              "SUPABASE_KEY=${SUPABASE_KEY}" \
              "SUPABASE_SERVICE_KEY=${SUPABASE_SERVICE_KEY}" \
              "SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}" \
              "SUPABASE_JWT_SECRET=${SUPABASE_JWT_SECRET}" \
              "NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}" \
              "NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}" \
              "POSTGRES_DATABASE=${POSTGRES_DATABASE}" \
              "POSTGRES_HOST=${POSTGRES_HOST}" \
              "POSTGRES_PASSWORD=${POSTGRES_PASSWORD}" \
              "POSTGRES_PRISMA_URL=${POSTGRES_PRISMA_URL}" \
              "POSTGRES_URL=${POSTGRES_URL}" \
              "POSTGRES_URL_NON_POOLING=${POSTGRES_URL_NON_POOLING}" \
              "POSTGRES_USER=${POSTGRES_USER}" \
              > "${APP_DIR}/.env"

      - name: Docker build and run
        uses: appleboy/ssh-action@v1.2.0
        env:
          APP_DIR: ${{ env.APP_DIR }}
          HOST_PORT: ${{ env.HOST_PORT }}
        with:
          host: ${{ vars.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ vars.DEPLOY_PORT || '22' }}
          command_timeout: 15m
          envs: APP_DIR,HOST_PORT
          script: |
            set -euo pipefail
            cd "${APP_DIR}"
            chmod +x scripts/docker-deploy-remote.sh
            export APP_DIR HOST_PORT
            ./scripts/docker-deploy-remote.sh

      - name: Health check
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ vars.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ vars.DEPLOY_PORT || '22' }}
          script: |
            set -euo pipefail
            for i in $(seq 1 30); do
              if curl -fsS "http://127.0.0.1:${{ env.HOST_PORT }}/" >/dev/null 2>&1; then
                echo "Health check passed"
                docker ps --filter name=info_platform
                exit 0
              fi
              sleep 2
            done
            docker logs info_platform --tail 80 2>&1 || true
            exit 1
